<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
  </head>
  <body>
    <% if(user.role != 'guest' && title != 'Login Page'){%>
      Welcome, <%= `${user.username} as ${user.role}  (` %><a href="/logout">logout</a>)<br><br>
    <%}%>
    <a href="/">Home</a> <a href="/teachers">Teachers</a> <a href="/subjects">Subjects</a> <a href="/students">Students</a>
    <h1><%= title %></h1>
